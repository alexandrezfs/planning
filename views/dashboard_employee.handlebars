<div class="row">

    <div class="col-lg-12">

        <h1>Vos plannings</h1>

        <table class="table table-bordered">
            {{#each plannings}}
                <tr>
                    <td><i class="fa fa-file"></i> {{this.name}}</td>
                    <td><a href="{{this.uri}}" target="_blank">Télécharger</a></td>
                </tr>
            {{/each}}
        </table>

    </div>

</div>